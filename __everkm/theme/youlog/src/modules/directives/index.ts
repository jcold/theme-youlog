import observeVisibility from './useObserveVisibility'
import clickOutside from './useClickOutside'

// 注册指令
export function registerDirectives() {
  // 在此处可以进行全局指令注册，如果框架支持的话
}

export {
  observeVisibility,
  clickOutside,
} 